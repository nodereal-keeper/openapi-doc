openapi: 3.1.0
info:
  title: eth_getCode - optimism
  version: '1.0'
servers:
  - url: 'https://opt-mainnet.nodereal.io/v1'
paths:
  /{apiKey}:
    post:
      $ref: ../ethereum/eth_getCode.yaml#/components/pathItems/path/post
      operationId: eth-getcode-optimism
      x-readme:
        samples-languages:
          - javascript
          - curl
          - python
          - go
